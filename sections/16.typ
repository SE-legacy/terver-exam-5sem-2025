#import "../conf.typ": *
#theorem([
  Пусть $limits(theta_1)^~$ и $limits(theta_2)^~$ --- 2 эффективные оценки
  параметра $theta$. Тогда $limits(theta_1)^~ = limits(theta_2)^~$, т.е
  эффективная оценка единственна.
], [])
